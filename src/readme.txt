you're gonna want to configure a .env file containing values for these before the site can actually be run, 

(no need to use quote markers though, it considers the values
as strings by default!)

SECRET_KEY=idk
DB_NAME=idk
DB_USER=idk
DB_PW=something
DB_HOST=else

